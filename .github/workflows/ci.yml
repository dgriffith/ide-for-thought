# CI for Minerva (#340 + #394).
#
# Two jobs run in parallel:
#   - lint-and-test: tsc + svelte-check + eslint + vitest (with coverage floors)
#   - e2e: electron-forge package + Playwright Electron smoke
#
# Both on macos-latest: matches the dev machine, and the chokidar
# watcher tests (#345) exercise macOS fsevents semantics — a Linux
# runner would need different timing tolerances. The e2e job
# additionally needs a darwin Electron app bundle, so cross-platform
# would mean separate package targets per platform.

name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel in-flight runs when a new commit lands on the same branch — saves
# minutes on a busy PR.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    runs-on: macos-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v7

      - name: Set up pnpm
        # Version comes from the `packageManager` field in package.json.
        uses: pnpm/action-setup@v6.0.10

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: pnpm

      # node-linker=hoisted → node_modules is a self-contained flat tree (no
      # store symlinks), so caching it directly is safe. Keyed on the lockfile
      # + Node + OS/arch, so a dep or Node bump busts it. The `cache: pnpm`
      # store cache above still speeds the miss path. Bump v1 if a runner-image
      # change ever stales the native binaries baked into the tree (#663).
      - name: Cache node_modules
        id: node-modules-cache
        uses: actions/cache@v6
        with:
          path: node_modules
          key: node-modules-v1-${{ runner.os }}-${{ runner.arch }}-node-${{ hashFiles('.nvmrc') }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install dependencies
        if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: pnpm install --frozen-lockfile

      - name: Lint (tsc + svelte-check + eslint)
        run: pnpm lint

      - name: Lint fonts
        run: pnpm lint:fonts

      # website/docs/*.html is generated from _layout.html + _nav.json +
      # _content/*.html fragments (scripts/build-docs.mjs). `--check` diffs the
      # regenerated pages against what's committed without writing, so a
      # hand-edit to a generated page that drifts from its source fragment
      # fails here instead of only in a developer's local predev/prebuild run.
      - name: Check generated docs are up to date
        run: pnpm check:docs

      # `pnpm coverage` runs the full suite with v8 instrumentation and enforces
      # the per-area thresholds in vitest.config (src/shared, src/main/llm,
      # src/main/notebase) — so the trust + security paths can't silently
      # regress (#679). The citeproc publish suites carry per-test timeouts
      # (#677) that absorb the instrumentation slowdown.
      - name: Test + coverage
        run: pnpm coverage

      # Trend + per-PR coverage-delta visibility (#1096 / QA H4); tuned by
      # `codecov.yml`. The floors above (pnpm coverage) remain the hard gate —
      # Codecov is informational and never blocks a PR (fail_ci_if_error: false).
      # Non-fatal so CI isn't blocked before the repo is enrolled / CODECOV_TOKEN
      # is set; the upload + PR comment activate automatically once it is.
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          files: ./coverage/lcov.info
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  # Supply-chain / CVE scanning (#1095 / QA H3). Paired with .github/dependabot.yml:
  # Dependabot opens update PRs, this step makes the current advisory state
  # visible on every PR/push. Lightweight + decoupled — audit reads only the
  # lockfile, so no node_modules install, no native binaries, ubuntu is fine.
  #
  # `audit:prod` (shipped surface) is now BLOCKING (#1455). The 8 high + 2
  # critical shipped advisories from the 2026-07-27 baseline were all in
  # transitive tooling unreachable at runtime — rdflib's git-URL resolver
  # (parse-url SSRF ×2, tmp, gry) and @huggingface/transformers' native/image
  # deps (sharp, adm-zip via onnxruntime-node, which our WASM-only voice +
  # embeddings paths never load) — and were force-patched via `pnpm.overrides`
  # (parse-url ≥8.1, tmp ≥0.2.6, gry ≥6, sharp ≥0.35, adm-zip ≥0.6). No
  # `--ignore` allowlist was needed; the overrides resolved cleanly and graph/
  # SPARQL/voice were verified (pnpm lint + test + e2e).
  #
  # `audit:all` (full tree incl. build/dev tooling) stays NON-BLOCKING — the
  # remaining high/critical live in @electron-forge/cli, eslint, vite, etc.,
  # which never reach users. STEP 2 of the promotion path: once the full tree
  # is clean, remove `continue-on-error` from `audit:all` too (or drop
  # `audit:prod` and gate `audit:all`).
  #
  # Remediation levers: bump via the Dependabot PRs; force a patched transitive
  # with a `pnpm.overrides` entry in package.json; allowlist an accepted/unfixable
  # advisory with `--ignore <CVE>` (append to the commands below with a comment
  # naming the advisory and why it's accepted).
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - name: Set up pnpm
        # Version comes from the `packageManager` field in package.json.
        uses: pnpm/action-setup@v6.0.10

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      # Shipped surface — the advisories that actually reach users. BLOCKING
      # (#1455): a new high/critical on a --prod dependency fails CI.
      - name: audit:prod (shipped deps, high+critical)
        run: pnpm audit --prod --audit-level=high

      # Full tree incl. build/dev tooling — visibility only until the whole
      # tree is clean.
      - name: audit:all (full tree, high+critical)
        continue-on-error: true
        run: pnpm audit --audit-level=high

  e2e:
    # Runs in parallel with lint-and-test. Stays out of the unit loop
    # because Electron boot is 5–10s and the watcher loop should stay
    # snappy.
    runs-on: macos-latest
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v7

      - name: Set up pnpm
        # Version comes from the `packageManager` field in package.json.
        uses: pnpm/action-setup@v6.0.10

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: pnpm

      # See lint-and-test for the rationale; same key so both jobs share the
      # cached tree across runs (#663).
      - name: Cache node_modules
        id: node-modules-cache
        uses: actions/cache@v6
        with:
          path: node_modules
          key: node-modules-v1-${{ runner.os }}-${{ runner.arch }}-node-${{ hashFiles('.nvmrc') }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install dependencies
        if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: pnpm install --frozen-lockfile

      - name: E2E (electron-forge package + Playwright)
        # `test:e2e` builds the app via electron-forge package, then
        # runs Playwright against the in-tree .vite/build/main.js
        # entry. Single test today (#394 smoke); add cases per
        # "the app stopped working entirely" incident.
        run: pnpm test:e2e

      # Flake visibility (#1946): Playwright retries a test twice in CI so a
      # transient Electron-boot hiccup doesn't fail the job, but nothing
      # previously aggregated how often that retry actually fired — a test
      # that needed one on every run was indistinguishable from one that
      # always passed first try. `if: always()` so this runs (and the rate
      # gets recorded) even when the job ultimately failed.
      #
      # Decision recorded here: a retry does NOT fail the job today (no
      # --max-flaky passed). Failing on any retry would defeat the reason
      # retries exist (#1097), and the actual flake rate isn't known yet —
      # this step's job right now is accumulating that history in the
      # Actions run summaries so a real numeric budget can be set later.
      - name: E2E flake report
        if: always()
        run: node scripts/e2e-flake-report.mjs playwright-report.json

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: playwright-report.json
          retention-days: 30
